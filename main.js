//Function for Discount Button
function buttonDiscount (){
    let origBudget = document.getElementById(budgetInput).value;
    //const discountValue = .2;
    let discountedPrice = Number(origBudget) * 0.2;
}